apiVersion: v1
kind: Service
metadata:
  name: sqlink-backend
  labels:
    app: sqlink-backend
spec:
  type: ClusterIP
  selector:
    app: sqlink-backend
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
